// This is mostly a simple map to list out the types of plays properly

export default function Plays_Window(props) {

    return (
        <div>
            <div>Box Selection List Of Plays</div>
            <div>haha you thought this is actually the predictions...</div>
            <div>I think i can just import a getter function... well see </div>
            <div>Im assuming we pass in the plays here and the state change here</div>
            <div>changes the state change in play_preview and in play_information</div>
            <div>Prediction output {props.gameState[8]}</div>
        </div>
    );
}